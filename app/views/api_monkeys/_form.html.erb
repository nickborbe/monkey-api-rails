<%= form_with(model: api_monkey, local: true) do |form| %>
  <% if api_monkey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_monkey.errors.count, "error") %> prohibited this api_monkey from being saved:</h2>

      <ul>
      <% api_monkey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
